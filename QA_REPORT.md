# 液体氯化钙线索系统验收记录

最近验收日期：2026-07-23

## 已发现并修复

- 数据库连接未统一关闭，长期运行可能积累连接。
- 同一监控任务可被重复启动，可能造成重复采集和重复提醒。
- 后台调度异常没有进入可查看的错误台账。
- 同步采集接口的错误没有持久化，刷新后无法追踪。
- 部分任务型结果会被错误写入企业线索库。
- 监控任务不存在时缺少明确错误响应。
- Render 使用临时磁盘时，重启可能丢失线索和销售记录。
- 系统版本号在窄屏下换行过多。
- 销售批量更新、液钙供需字段和完整数据库备份缺失。
- 竞品“反向开发”切换模式后残留上一轮进度和导出状态。
- 本地网站使用临时 launchctl 任务，重启或权限变化后可能打不开。
- LaunchAgent 直接读取 Documents 下脚本时，会被 macOS 文件访问权限拦截。
- 重复采集会把旧综合分再次作为基础分，导致同一企业分数逐次上涨。
- “山东省济宁市”一类省市组合名称会被地图接口忽略城市限制，混入全国结果。
- 下游精准采集会混入商店、住宅、道路等非目标 POI。
- 同一企业后采集到的新电话或数据来源会覆盖旧值。
- 企业地图采集只支持高德，单一数据源失效或覆盖不足时无法自动补充。
- 外部采购平台或企业官网超时时，采集器会继续批量请求同一失效来源，导致整批等待过长。
- 同一个外部访问错误会重复写入系统运行中心，掩盖真正需要处理的配置或数据库错误。
- 手动采集与定时监控对“已跳过的外部来源”使用不同错误等级，容易误判为系统故障。

## 已完成回测

- 密码、手机号和验证码登录。
- 四种采集方向切换。
- 线索数据库加载、筛选和自动去重。
- 批量修改销售状态、负责人和下次跟进时间。
- 液钙浓度、月供/用量、杂质、运输半径、储运条件和商业价值保存。
- 系统错误记录、处理状态、数据源健康和操作记录。
- 数据库手动备份、每日自动备份和备份保留数量。
- 390 像素手机宽度下无页面级横向溢出。
- 当前前端版本无浏览器控制台错误。
- 竞品公开页面识别、行业关键词提取和反向开发建议。
- 真实回测“山东 + 液体氯化钙 + 1688”，成功识别供应商并定位企业官网。
- 自动回归测试 53 项全部通过。
- 稳定评分、A-D 证据分级、多来源联系方式合并和省市硬校验已加入回归测试。
- `/health` 仅返回高德、百度、天地图是否已配置的布尔状态，不暴露任何 API 密钥。
- 天地图行政区域搜索 V2.0 已加入请求参数、分页、错误映射、POI 字段映射、
  单源运行、官网候选发现和密钥不泄露回归测试。
- 百度千帆网页搜索已加入 Bearer 鉴权、响应字段映射、7 天持久缓存、
  每日额度保护和接口失败自动回退测试。
- 百度千帆真实回测成功：含氟环评检索生成 2 家企业线索；重复查询命中缓存，
  不增加调用次数；结构化站点过滤成功召回并解析抖音公开索引。
- 官网识别已排除黄页、B2B、招聘和企业查询目录；真实回测将山东东岳化工
  正确定位到 `dongyuechem.com`。
- 百度地点检索 3.0 真实回测成功：济南水处理单源 23 家企业、8 家带电话且无错误。
- 高德与百度双源真实回测成功：43 家企业、10 家带电话，并验证跨源企业合并。
- 真实回测“山东省济宁市 + 水处理 + 精准模式”，结果地区全部属于济宁市。
- 390 像素手机视口下复测无页面级横向溢出，浏览器控制台无错误。
- 本地服务已改为正式 macOS LaunchAgent，支持登录后自动启动和崩溃自恢复。
- 本地修复脚本、安装脚本和一键打开脚本已加入 `scripts/`。
- 已完成故障演练：手动杀掉网站进程后，服务自动重启并通过健康检查。
- 外部平台已加入首请求探测、临时故障重试和按策略限额；单个平台不可用时会快速跳过，
  不再发起几十次注定失败的请求。
- 企业官网环保核验和官网采购扫描已按精准、均衡、广覆盖分别限制为 10、16、24 家。
- 相同未处理错误会原位更新时间，不再重复刷屏；成功获得其他线索时，外部来源跳过记为信息。

## 外部限制

- 企业电话、联系人和工艺信息只能来自合法公开信息，无法保证每家企业都有电话。
- 政府采购、公共资源和企业官网可能存在限流、验证码、页面改版或临时不可用。
- 高德和百度地图结果偏向地图登记主体，仍需结合官网、环评、排污许可和招采证据核验。
- 生产短信登录仍需要有效短信服务参数；本地开发模式只用于测试。
